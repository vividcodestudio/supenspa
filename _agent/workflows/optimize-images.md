---
description: Optimize images in the project using sharp
---

1. Ensure `sharp` is installed in the environment.
// turbo
2. Run the image optimization script: `node /tmp/image-optimizer/stats.mjs`.
3. Review the output stats.
4. If satisfied, manually move files from `/tmp/supenspa-optimized` to `website-pages/assets/images`.
